---
import { Editor } from "@components/posts/Editor";
import { gatherEditorFiles } from "@utils/gatherEditorFiles";

const files = await gatherEditorFiles(import.meta.url);
---

<Editor files={files} client:visible />
