---
import { Editor } from "@components/Editor";
import { gatherEditorFiles } from "@utils/gatherEditorFiles";

const files = await gatherEditorFiles(Astro.url.pathname);
---

<Editor files={files} activeFile="app.css" client:visible />
